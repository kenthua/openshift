apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "{{ pv_name }}" 
spec:
  capacity:
    storage: "{{ capacity }}Gi" 
  accessModes:
    - ReadWriteOnce
  nfs: 
    path: "{{ vol_path }}" 
    server: "{{ vol_hostname }}"
